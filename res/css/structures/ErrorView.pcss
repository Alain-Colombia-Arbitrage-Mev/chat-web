/*
Copyright 2020-2024 New Vector Ltd.

SPDX-License-Identifier: AGPL-3.0-only OR GPL-3.0-only OR LicenseRef-Element-Commercial
Please see LICENSE files in the repository root for full details.
*/

.mx_ErrorView {
    --width: 520px;
    --cpd-separator-inset: calc(50% - (var(--width) / 2));
    --cpd-separator-spacing: var(--cpd-space-8x);

    font-family:
        -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif, "Apple Color Emoji",
        "Segoe UI Emoji", "Segoe UI Symbol";
    text-align: center;
    color: white;
    width: 100%;
    height: 100vh;
    min-height: 100vh;
    box-sizing: border-box;
    overflow: auto;
    padding: var(--cpd-space-8x);
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;

    background-color: #1B1E43;
    background-image: none;
    position: relative;

    .mx_ErrorView_logo {
        display: block;
        margin: 0 auto var(--cpd-space-6x);
        max-width: 200px;
        height: auto;
    }

    .mx_ErrorView_container {
        max-width: var(--width);
        margin: 0 auto var(--cpd-space-8x);
        z-index: 2;
        position: relative;
    }

    h1,
    h2 {
        color: white;
    }

    h2 {
        margin: var(--cpd-space-8x) 0;
        font-size: var(--cpd-font-size-heading-sm);
    }

    p {
        color: rgba(255, 255, 255, 0.8);
    }

    .mx_ErrorView_flexContainer {
        margin: 0 auto;
        max-width: max-content;
        flex-wrap: wrap;
        justify-content: space-evenly;
    }

    .mx_ErrorView_buttons {
        margin-top: var(--cpd-space-6x);
    }

    /* Imagen animada de la mujer - aparece debajo del mensaje específico */
    .mx_ErrorView_woman_inline {
        display: block;
        margin: var(--cpd-space-4x) auto;
        max-width: 250px;
        height: auto;
        opacity: 0;
        animation: slideUpFadeIn 1.5s ease-out forwards;
    }

    @keyframes slideUpFadeIn {
        0% { 
            transform: translateY(50px);
            opacity: 0;
        }
        100% { 
            transform: translateY(0);
            opacity: 0.8;
        }
    }

    /* Responsive design */
    @media screen and (max-width: 768px) {
        .mx_ErrorView {
            padding: var(--cpd-space-4x);
            height: 100vh;
        }

        .mx_ErrorView_logo {
            max-width: 150px;
            margin-bottom: var(--cpd-space-4x);
        }

        .mx_ErrorView_container {
            --width: 90vw;
            max-width: 400px;
        }

        .mx_ErrorView_woman_inline {
            max-width: 200px;
        }

        h1 {
            font-size: 1.5rem;
        }

        p {
            font-size: 0.9rem;
        }
    }

    @media screen and (max-width: 480px) {
        .mx_ErrorView {
            padding: var(--cpd-space-2x);
        }

        .mx_ErrorView_logo {
            max-width: 120px;
        }

        .mx_ErrorView_container {
            --width: 95vw;
        }

        .mx_ErrorView_woman_inline {
            max-width: 150px;
        }

        h1 {
            font-size: 1.25rem;
        }

        p {
            font-size: 0.85rem;
        }
    }

    /* Vertical centering for all screen sizes */
    @media screen and (min-height: 600px) {
        .mx_ErrorView {
            justify-content: center;
            align-items: center;
        }
    }
}
